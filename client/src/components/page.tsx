import { NextPage } from "next";
import { redirect } from "next/navigation";
import React from "react";

const page: NextPage = () => {
  // redirect("/login");
  return <div>page</div>;
};

export default page;
